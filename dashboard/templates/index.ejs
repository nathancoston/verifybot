<!DOCTYPE html>
<html lang="en">

<%- include("blocks/head.ejs") %>

<body>
  <% if (perms.level >= 2) { %>
    <nav class="navbar navbar-expand-md navbar-dark bg-info">
      <a class="navbar-brand" href="staff"><span class="fa fa-lock"></span> Staff Panel</a>
    </nav>
  <% } %>

  <div class="container mt-3">
    <% if (data.error) { %>
      <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <strong>Error:</strong> <%- data.error %>
        <button type="button" class="close d-none d-md-block" data-dismiss="alert" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
    <% } %>
    <% if (data.message) { %>
      <div class="alert alert-primary alert-dismissible fade show" role="alert">
        <%- data.message %>
        <button type="button" class="close d-none d-md-block" data-dismiss="alert" aria-lable="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
    <% } %>
    <% if (client.guild.onLockdown) { %>
      <form method="post">
        <div class="form-group">
          <label for="username">Minecraft Username</label>
          <input type="text" class="form-control" name="username">
        </div>
        <div class="form-group">
          <label for="key">Secret Key</label>
          <input type="text" class="form-control" name="key">
        </div>
        <br><button type="submit" class="btn btn-info verify"><span class="fa fa-check"></span> Verify</button><br>
        <b>Don't have a secret key? Get one!</b>
        <ol>
          <li>Log onto the <kbd>mcdiamondfire.com</kbd> minecraft server.</li>
          <li>Type <kbd>/verify</kbd> in chat to get your secret key.</li>
          <li>Enter that secret key in the box above.</li>
          <li>You'll get sent a confirmation link via private message on Discord.</li>
          <li>Click the link given, and your account will be verified!</li>
        </ol>
      </form>
    <% } else { %>
      <h1>The server is currently on lockdown!</h1>
      This means that you are not currently able to verify your account. Please try again later.
    <% } %>
  </div>

  <%- include("blocks/footer.ejs") %>
</body>

</html>